apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deputyl.fullname" . }}-test-connection"
  labels:
    {{- include "deputyl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deputyl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
